# ATChess Production Environment Configuration
# Copy this file to /etc/atchess/protocol.env and /etc/atchess/web.env on your server
# Update the values according to your deployment

# ====================
# Protocol Service Configuration (/etc/atchess/protocol.env)
# ====================

# AT Protocol Configuration
ATCHESS_ATPROTO_PDS_URL=https://bsky.social
ATCHESS_ATPROTO_HANDLE=your-atchess-bot.bsky.social
ATCHESS_ATPROTO_PASSWORD=your-app-password-here
ATCHESS_ATPROTO_USE_DPOP=false

# Server Configuration
ATCHESS_SERVER_HOST=0.0.0.0
ATCHESS_SERVER_PORT=8080

# Development Configuration
ATCHESS_DEVELOPMENT_DEBUG=false
ATCHESS_DEVELOPMENT_LOG_LEVEL=info

# Firehose Configuration (optional)
ATCHESS_FIREHOSE_ENABLED=false
ATCHESS_FIREHOSE_URL=wss://bsky.social/xrpc/com.atproto.sync.subscribeRepos

# ====================
# Web Service Configuration (/etc/atchess/web.env)
# ====================

# Server Configuration
ATCHESS_SERVER_HOST=0.0.0.0
ATCHESS_SERVER_PORT=8081

# Protocol Service URL (internal communication)
ATCHESS_PROTOCOL_URL=http://localhost:8080

# Public URL (for OAuth callbacks and external references)
ATCHESS_PUBLIC_URL=https://atchess.example.com

# Session Configuration
ATCHESS_SESSION_SECRET=generate-a-secure-random-string-here
ATCHESS_SESSION_SECURE=true

# Development Configuration
ATCHESS_DEVELOPMENT_DEBUG=false
ATCHESS_DEVELOPMENT_LOG_LEVEL=info

# ====================
# Notes
# ====================
# 1. Generate app password at: https://bsky.app/settings/app-passwords
# 2. Create a dedicated bot account for ATChess
# 3. Use strong, unique session secret (generate with: openssl rand -hex 32)
# 4. Update ATCHESS_PUBLIC_URL to match your domain
# 5. Keep these files secure with proper permissions (chmod 600)